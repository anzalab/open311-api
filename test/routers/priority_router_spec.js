'use strict';

/**
 * Priority router specification
 *
 * @description :: Server-side router specification for Priority
 */

//dependencies


describe('Priority Router', function () {
  it('should handle HTTP POST on /priorities');
  it('should handle HTTP GET on /priorities/:id');
  it('should handle HTTP PUT on /priorities/:id');
  it('should handle HTTP PATCH on /priorities/:id');
  it('should handle HTTP GET on /priorities');
  it('should handle HTTP DELETE on /priorities/:id');
});
