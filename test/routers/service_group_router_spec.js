'use strict';

/**
 * ServiceGroup router specification
 *
 * @description :: Server-side router specification for ServiceGroup
 */

//dependencies


describe('ServiceGroup Router', function () {
  it('should handle HTTP POST on /servicegroups');
  it('should handle HTTP GET on /servicegroups/:id');
  it('should handle HTTP PUT on /servicegroups/:id');
  it('should handle HTTP PATCH on /servicegroups/:id');
  it('should handle HTTP GET on /servicegroups');
  it('should handle HTTP DELETE on /servicegroups/:id');
});
