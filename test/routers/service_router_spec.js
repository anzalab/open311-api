'use strict';

/**
 * Service router specification
 *
 * @description :: Server-side router specification for Service
 */

//dependencies


describe('Service Router', function () {
  it('should handle HTTP POST on /services');
  it('should handle HTTP GET on /services/:id');
  it('should handle HTTP PUT on /services/:id');
  it('should handle HTTP PATCH on /services/:id');
  it('should handle HTTP GET on /services');
  it('should handle HTTP DELETE on /services/:id');
});
