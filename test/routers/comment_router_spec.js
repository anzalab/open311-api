'use strict';

/**
 * Comment router specification
 *
 * @description :: Server-side router specification for Comment
 */

//dependencies


describe('Comment Router', function () {
  it('should handle HTTP POST on /comments');
  it('should handle HTTP GET on /comments/:id');
  it('should handle HTTP PUT on /comments/:id');
  it('should handle HTTP PATCH on /comments/:id');
  it('should handle HTTP GET on /comments');
  it('should handle HTTP DELETE on /comments/:id');
});
